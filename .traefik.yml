displayName: Geo Access Control
type: middleware

import: github.com/yourusername/geo-access-control

summary: 'Block or allow requests based on geographic location (country, region, city)'

testData:
  allowedCountries:
    - US
    - CA
    - GB
  api: "http://geoip-api:8080/country/{ip}"
