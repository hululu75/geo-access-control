displayName: Geo Access Control
type: middleware

import: github.com/hululu75/geo-access-control

summary: 'Block or allow requests based on geographic location (country, region, city)'

testData:
  geoAPIEndpoint: "http://geoip-api:8080/country/{ip}"
  accessRules:
    US: true
    CA: true
    GB: true
